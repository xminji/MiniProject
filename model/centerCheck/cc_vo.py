
class centerCheck:
    def __init__(self, id, address, centerName, centerType, createdAt, facilityName, lat, lng, org, phoneNumber, sido, sigungu, updatedAt, zipCode):
        self.id = id
        self.address = address
        self.centerName = centerName
        self.centerType = centerType
        self.createdAt = createdAt
        self.facilityName = facilityName
        self.lat = lat
        self.lng = lng
        self.org = org
        self.phoneNumber = phoneNumber
        self.sido = sido
        self.sigungu = sigungu
        self.updatedAt = updatedAt
        self.zipCode = zipCode



    # def __str__(self): #ToString
    #     return 'id ' + self.id + '\n'+ 'address ' + self.address + '\n'+ 'centerName ' + self.centerName + '\n'+ 'centerType ' + self.centerType + '\n'+'createdAt ' + self.createdAt + '\n' + 'facilityName ' + self.facilityName + '\n'+ 'lat ' + self.lat + '\n'+ 'lng ' + self.lng + '\n'+'org ' + self.org + '\n' + 'phoneNumber ' + self.phoneNumber + '\n'+ 'sido ' + self.sido + '\n'+ 'sigungu ' + self.sigungu + '\n'+'updatedAt ' + self.updatedAt + '\n' + 'zipCode ' + self.zipCode + '\n'





